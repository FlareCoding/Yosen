name: CPP Build Check

on: push

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
    - uses: actions/checkout@v2
    - name: generate project
      run: cd scripts && .\GenerateProject.bat && cd ..\build
    - name: build project
      run: cmake --build .
